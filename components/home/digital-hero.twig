
<section class="hero" style="background-image:url('{{ field('bg_image') }}')">
  <div class="overlay container">
    <h1>{{ field('title') }}</h1>
    <p class="muted">{{ field('subtitle') }}</p>
    <a href="{{ field('cta_link') }}" class="btn">{{ field('cta_label') }}</a>
  </div>
</section>
