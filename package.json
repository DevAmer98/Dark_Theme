{
  "name": "dark-theme",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preview": "salla theme preview",
    "package": "salla theme package",
    "development": "node -e \"console.log('ready')\"",
    "watch": "node -e \"console.log('watch noop');setInterval(()=>{},1<<30)\""
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}